AttoFTP Demo
============

This is the root of a demo FTP server. Anonymous users land
in /pub. Named users land in their own /users/<name> folders.

Try the following commands once connected:

  ls          (list current dir)
  cd pub      (change into /pub)
  get hello.txt  (download a file)
  bye         (disconnect)

